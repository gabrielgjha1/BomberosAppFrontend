import { CambiarnombrePipe } from './cambiarnombre.pipe';

describe('CambiarnombrePipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarnombrePipe();
    expect(pipe).toBeTruthy();
  });
});
